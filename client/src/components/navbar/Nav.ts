import styled from 'styled-components';

const Nav = styled.nav`
  li {
    display: flex;
    align-items: center;
    column-gap: 20px;

    span {
      cursor: pointer;
    }
  }
`;

export default Nav;